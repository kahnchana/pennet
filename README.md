Run the file `self_supervised.py` for training, and `test_self_supervised.py` testing. 
Paths are set for using CIFAR10 dataset. PASCAL pretrained pennet model is used. 

A multi-class SVM is used for training. 